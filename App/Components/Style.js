import { StyleSheet, Text, View } from 'react-native'
import React from 'react'



const fontStyle = StyleSheet.create({
    boldTitle: {
        fontWeight:'800', fontSize:20, color:'black'
    }
})

export { fontStyle }